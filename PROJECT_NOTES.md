# 项目问题与修改记录

> 记录开发过程中遇到的所有技术问题、产品需求变更和修复过程，供面试复盘参考。

## 一、RAG 基础阶段

### 1. 纯向量检索召回率低
- **问题**：纯 bge 向量检索 Top-4 召回率只有 63.2%，很多精确关键词匹配不到
- **解决**：加 BM25 关键词检索（jieba 中文分词），双路 RRF 融合
- **效果**：Top-4 召回率提升到 94.7%（+31.6pp）
- **典型案例**："怎么知道自己是几班的"，纯向量漏检，BM25 通过"几班"关键词成功召回

### 2. HuggingFace 模型下载慢/卡住
- **问题**：国内直连 HuggingFace 下载 bge 模型超时
- **解决**：设环境变量 `HF_ENDPOINT=https://hf-mirror.com` 走国内镜像

### 3. 模型把工具调用格式写在正文里
- **问题**：DeepSeek 偶尔把 DSML 格式的工具调用（`<||DSML||>...invoke name=...`）直接吐给用户
- **解决**：写了 `parse_dsml_tool_calls()` 正则解析器兜底，统一成 `[{id, name, arguments(dict)}]` 格式

## 二、Agent 阶段

### 4. 问最高分却答最低分
- **问题**：用户问"计算机最高分多少"，Agent 返回的是最低分
- **根因**：`admission.py` 只返回了最低分字段
- **解决**：一次返回最高/最低/平均/位次全套字段

### 5. 同一个问题让 LLM 答两遍
- **问题**：`stream=False` 那次已经拿到了 `msg.content`（最终答案），代码又调 `_stream_final()` 让模型重新生成一遍
- **解决**：第一遍拿到答案就直接流式吐出，不再重调，省一次 API 调用

### 6. 冲稳保打包票
- **问题**：Agent 会说"你稳上""肯定录取"
- **解决**：系统提示加规则，只说"高于历史最低分"+风险提示，不许说"稳上/没悬念"

### 7. 用户回"可以/好/要"不执行上轮提议
- **问题**：Agent 问"要不要帮你看看550分还能冲哪些专业"，用户回"可以"，Agent 没反应
- **解决**：系统提示加规则，肯定词直接执行上轮动作，不再反问

### 8. 追问没记忆
- **问题**：问"计算机报考条件"有记忆，追问"学费呢"就没记忆了
- **根因**：多轮记忆收敛逻辑有 bug
- **解决**：每轮结束干净地追加 [user, assistant] 对，保留最近6轮

### 9. 不根据左侧报考信息回答
- **问题**：用户填了省份/分数/意向，但 Agent 回答时没用到这些信息
- **解决**：每轮把考生画像注入 system prompt

## 三、产品/UI 迭代

### 10. 标题和导航
- 顶部黑色标题改为"NDKY 招生咨询"
- 左侧导航栏只显示"问题记录"
- 输入框从顶部 `st.text_input` 改成底部 `st.chat_input`（聊天风格）

### 11. 回答格式
- 去掉"根据参考资料"和括号里的引用说明
- 参考依据里学生姓名打码（黄**）
- 咨询人姓名日期字体调小、更透明，放在问题和答案下面
- 查不到的资料不显示参考依据
- 同一个问题多个答案时，优先选时间更近的回复

### 12. 主校区位置错误
- **问题**：Agent 说主校区在杭州湾新区
- **核实**：查2026招生章程，官方地址是"慈溪市白沙路街道文蔚路521号"（周巷校区：周巷镇和通路588号）
- **解决**：`school.py` 硬编码正确地址

### 13. 不要追问和示例问题
- 去掉"您还可以问"等推荐问题
- 去掉输入框占位符里的示例文字

### 14. 历史对话显示
- 按时间顺序显示全部历史对话
- 问完问题输入框清空
- 第二个问题自动往下放

## 四、数据阶段

### 15. 爬虫抓取
- 从招生网抓 2020–2026 年共 2158 条真实问答
- 从"历年信息"栏目爬 1187 条结构化录取记录（2023–2025，浙江+17省外）
- 处理了反爬：代理、延时、分页

### 16. 数据脱敏
- **问题**：知识库含真实学生姓名，GitHub 公开前要脱敏
- **解决**：正则匹配 `## 咨询 N（姓名，日期）`，把姓名改成"姓**"，单字名改成"*"
- 重建索引确保 chunks.json 里也没有真实姓名

## 五、部署阶段

### 17. Streamlit Cloud Secrets 不生效
- **问题**：在 Streamlit Secrets 里配了 `LLM_API_KEY`，但代码读不到
- **根因**：Streamlit Secrets 存在 `st.secrets` 里，不会自动变成环境变量
- **解决**：app.py 开头加代码，把 `st.secrets` 同步到 `os.environ`

### 18. requirements.txt 不完整
- **问题**：sentence-transformers 被注释掉了，Streamlit Cloud 装不上依赖
- **解决**：补全依赖列表，打开 sentence-transformers

### 19. index/ 目录被 gitignore
- **问题**：向量索引在 .gitignore 里，GitHub 上没有，Streamlit Cloud 要重新 ingest
- **解决**：把 index/（共5MB）加入 git，直接 push 上去省冷启动时间

## 六、简历相关

### 20. 召回率数据更新
- 评测集19条，混合检索 Top-4 从最初的 89.5% 提升到 94.7%
- 简历用最新数字：94.7%，较纯向量 +31.6pp
